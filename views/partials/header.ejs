<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Sử dụng biến `title` được truyền từ controller -->
    <title><%= title %> | BookStore của Người Anh Em</title>
    <!-- Link đến file CSS trong thư mục public -->
    <link rel="stylesheet" href="/css/style.css">
    <!-- Có thể thêm các link CSS của Bootstrap, FontAwesome... ở đây -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container">
                <a class="navbar-brand" href="/">📖 BookStore</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                     <ul class="navbar-nav ms-auto align-items-center">
                        <li class="nav-item">
                             <a class="nav-link" href="/">Trang Chủ</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/products">Sản Phẩm</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/cart">Giỏ Hàng</a>
                        </li>
                        <!-- Thêm một thẻ script nhỏ để xử lý logic này -->
                        <div id="auth-links" class="d-flex">
                        <!-- Các link sẽ được JS chèn vào đây -->
                        </div>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main class="container my-4">